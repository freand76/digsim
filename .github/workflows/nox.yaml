name: Nox

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup Nox
      uses: daisylb/setup-nox@v2.1.0
    - name: Run Nox
      run: |
        nox -s lint test
